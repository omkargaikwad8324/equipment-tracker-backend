spring.application.name=equipmenttracker

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/equipment_tracker
spring.datasource.username=root
spring.datasource.password=root

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# Optional
spring.jpa.open-in-view=false

# ==========================
# JWT Configuration
# ==========================
jwt.secret=ShivSwarajyaEarthMoversSuperSecretKey2026ForJWTAuthentication
jwt.expiration=86400000